---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/build-planner/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/echo 'Dead Signal cPanel deploy engine test' > $DEPLOYPATH/cpanel-deploy-test.txt
    - /bin/echo 'TOKEN: DS-DEPLOY-TEST-2201' >> $DEPLOYPATH/cpanel-deploy-test.txt
    - /bin/echo 'If you can read this file, Deploy HEAD Commit executed successfully.' >> $DEPLOYPATH/cpanel-deploy-test.txt
