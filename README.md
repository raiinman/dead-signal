# Dead Signal

Dead Signal is an independent Once Human community planner and database project.

The Ultimate Planner is deployed from the `main` branch through cPanel Git Version Control. The deployment reconstructs the existing v1.2 site bundle, applies deployment patches through v1.2.8, then applies the current Build Lab visual overlay while v1.3 corpus work continues.

Production planner: `/build-planner/`
