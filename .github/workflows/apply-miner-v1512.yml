name: Apply Miner v1.5.12 source patch

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch transport is being restaged."
